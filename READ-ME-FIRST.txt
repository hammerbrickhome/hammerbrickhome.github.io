HAMMER BRICK — EMERGENCY WEBSITE FIX

FIRST FIX — USE THIS:
  script.js

Replace ONLY the root script.js in GitHub with this fixed file.

Do not delete:
- .pages.yml
- gallery.json
- images/
- site-data/
- content/
- your new pages

WHY:
The original Admin V4 script used a MutationObserver that could repeatedly
rewrite the chat text and make the browser hang. This script removes that loop
while preserving the V4 admin/navigation/business-settings features.

AFTER COMMIT:
1. Wait 30-90 seconds.
2. Open www.hammerbrickhome.com in a private/incognito window.
3. Hard refresh:
   Mac: Command + Shift + R
   Windows: Ctrl + Shift + R

FALLBACK:
If the site still hangs after the fixed script has deployed, the ZIP also contains:
  FALLBACK-pre-V4-script.js

That is the known-working script from before the V4 expansion.
Do NOT use the fallback unless the fixed V4 script still fails.
